# Furnace web UI

Next.js app router front-end for the Furnace control plane.

## Environment

- `NEXT_PUBLIC_API_BASE` — control-plane base URL, defaults to `http://localhost:8000`

## Run

```bash
npm install
npm run dev
```

## Check

```bash
npm run lint
npm run build
```
